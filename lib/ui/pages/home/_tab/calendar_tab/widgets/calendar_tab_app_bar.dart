import 'package:flutter/material.dart';

class CalendarTabAppBar extends StatelessWidget {
  const CalendarTabAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      title: const Text('캘린더'),
    );
  }
}
